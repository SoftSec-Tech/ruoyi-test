<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysKnowledgeCategoryMapper">

    <resultMap type="SysKnowledgeCategory" id="SysKnowledgeCategoryResult">
        <result property="categoryId"   column="category_id"   />
        <result property="categoryName" column="category_name" />
        <result property="parentId"    column="parent_id"    />
        <result property="orderNum"     column="order_num"     />
        <result property="status"       column="status"        />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"      column="remark"        />
    </resultMap>

    <sql id="selectCategoryVo">
        select category_id, category_name, parent_id, order_num, status, create_by, create_time, update_by, update_time, remark
        from sys_knowledge_category
    </sql>

    <select id="selectCategoryById" parameterType="Long" resultMap="SysKnowledgeCategoryResult">
        <include refid="selectCategoryVo"/>
        where category_id = #{categoryId}
    </select>

    <select id="selectCategoryList" parameterType="SysKnowledgeCategory" resultMap="SysKnowledgeCategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name like concat('%', #{categoryName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
        </where>
        order by parent_id, order_num, category_id
    </select>

    <insert id="insertCategory" parameterType="SysKnowledgeCategory">
        insert into sys_knowledge_category (
            <if test="categoryName != null and categoryName != ''">category_name,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="orderNum != null">order_num,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        ) values (
            <if test="categoryName != null and categoryName != ''">#{categoryName},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="orderNum != null">#{orderNum},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateCategory" parameterType="SysKnowledgeCategory">
        update sys_knowledge_category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>

    <delete id="deleteCategoryById" parameterType="Long">
        delete from sys_knowledge_category where category_id = #{categoryId}
    </delete>

    <select id="countSnippetsByCategoryId" parameterType="Long" resultType="int">
        select count(1) from sys_knowledge_snippet where category_id = #{categoryId}
    </select>
</mapper>
